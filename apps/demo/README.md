# Demo consumer

This private workspace is reserved for the workshop demo consumer. The maintained runnable example is currently the framework-free [plain HTML integration](../../examples/plain-html/README.md).

Keep product-specific demo code here rather than adding it to the publishable SDK package.
